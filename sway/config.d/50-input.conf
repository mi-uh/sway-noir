# Disable pointer acceleration for all mice and keep the verified speed.
input type:pointer {
    accel_profile flat
    pointer_accel -0.15
}

# Use the same acceleration-free profile for the touchpad.
input type:touchpad {
    accel_profile flat
    pointer_accel -0.15
    tap enabled
    tap_button_map lrm
}
