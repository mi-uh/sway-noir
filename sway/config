# Sway Noir standalone configuration.
# Start it with: ~/.config/sway-noir/start-sway-noir

set $mod Mod4
set $left h
set $down j
set $up k
set $right l

# Sway's upstream default. Change this to use another terminal.
set $term foot
set $menu fuzzel --config=$SWAY_NOIR_DIR/fuzzel/fuzzel.ini

# Session commands can be overridden in 99-local.conf.
set $lock_command swaylock --daemonize --config $SWAY_NOIR_DIR/swaylock/config --image $SWAY_NOIR_DIR/swaylock/cozy-cyberpunk-room-lock.png --scaling fill
set $gammastep_command gammastep -c $SWAY_NOIR_DIR/gammastep/config.ini

include config.d/*.conf
