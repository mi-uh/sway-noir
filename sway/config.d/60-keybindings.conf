# Manual locking without changing the user's idle or suspend policy.
# Super+l remains available for focusing right.
bindsym $mod+Shift+x exec swaylock --config swaylock/config --image swaylock/cozy-cyberpunk-room-lock.png --scaling fill
